# `dockle` — see source
See `src/sast_tools/dockle.rs` for invocation defaults and parser behavior.
Install: see `scorchkit doctor` remediation hint.
This wrapper is part of WORK-114 (container/cloud batch) and ships in v2.1.
