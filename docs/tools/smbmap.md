# `smbmap` — see source

Operator-facing reference: see `src/tools/smbmap.rs` for invocation defaults, parser behavior, and finding shapes.

Install: see `scorchkit doctor` remediation hint or `docs/tools-checklist.md`.

This wrapper is part of WORK-111 (network/infra tool batch) and ships in v2.1.
