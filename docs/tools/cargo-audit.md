# `cargo-audit` — see source

Operator-facing reference: see `src/sast_tools/cargo_audit.rs` for invocation defaults, parser behavior, and finding shapes.

Install: see `scorchkit doctor` remediation hint.

This wrapper is part of WORK-113 (SAST expansion batch) and ships in v2.1.
