# `kics` — see source

Operator-facing reference: see `src/sast_tools/kics.rs` for invocation defaults, parser behavior, and finding shapes.

Install: see `scorchkit doctor` remediation hint.

This wrapper is part of WORK-113 (SAST expansion batch) and ships in v2.1.
