# Retired module pipeline template

This Claude/Forge-era template is no longer a workflow entry point. Create module work through:

```bash
bash bin/pipeline.sh create SLUG feature TITLE...
```

The script owns the canonical templates under `docs/planning/_templates`,
`docs/planning/pipeline/_templates`, and `docs/planning/knowledge/aar/TEMPLATE.md`.
