# Retired work pipeline template

This Claude/Forge-era template is no longer a workflow entry point. Create implementation work
through:

```bash
bash bin/pipeline.sh create SLUG TYPE TITLE...
```

The script owns the canonical templates under `docs/planning/_templates`,
`docs/planning/pipeline/_templates`, and `docs/planning/knowledge/aar/TEMPLATE.md`.
